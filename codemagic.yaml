workflows:
  ios-release:
    name: Build iOS (IPA) for Lakhsly
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      vars:
        API_BASE_URL: "https://dummy.ngrok-free.app"
    scripts:
      - name: إعداد توكن Mapbox
        script: |
          if [ -n "$MAPBOX_DOWNLOADS_TOKEN" ]; then
            echo "machine api.mapbox.com login macos password $MAPBOX_DOWNLOADS_TOKEN" > ~/.netrc
            chmod 0600 ~/.netrc
          fi
      - name: تنظيف المشروع
        script: flutter clean
      - name: تحميل الحزم و CocoaPods
        script: |
          flutter pub get
          cd ios && pod install && cd ..
      -  name: بناء التطبيق وتجهيز IPA لـ Sideloadly
         script: |
          # كتابة إعدادات التوقيع والـ Team في ملف Release.xcconfig قبل البناء
          echo "DEVELOPMENT_TEAM=1234567890" >> ios/Flutter/Release.xcconfig
          echo "CODE_SIGNING_ALLOWED=NO" >> ios/Flutter/Release.xcconfig
          echo "CODE_SIGNING_REQUIRED=NO" >> ios/Flutter/Release.xcconfig

          # بناء التطبيق للأجهزة الحقيقية بدون توقيع رقمي
          flutter build ios --release --no-codesign \
            --dart-define=API_BASE_URL=$API_BASE_URL \
            --obfuscate \
            --split-debug-info=build/debug-info

          # تحويل Runner.app إلى ملف app-unsigned.ipa
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r app-unsigned.ipa Payload
    artifacts:
      - build/ios/iphoneos/*.ipa
    publishing:
      email:
        recipients:
          - abdelrahmansoliman101@gmail.com