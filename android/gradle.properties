org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# MAPBOX_DOWNLOADS_TOKEN - set via environment variable MAPBOX_DOWNLOADS_TOKEN or in local.properties (not committed)
# To set locally, create a local.properties file in the android/ directory with:
# MAPBOX_DOWNLOADS_TOKEN=your_token_here
# Or set the environment variable before building
MAPBOX_DOWNLOADS_TOKEN=
