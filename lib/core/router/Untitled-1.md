You are the lead software architect for this Flutter project.

The project is currently in a partially refactored and inconsistent state.

Your first responsibility is to RECOVER the project before adding new features.

DO NOT create a new Flutter project.

DO NOT rewrite everything from scratch.

======================================================
PHASE 1 – PROJECT RECOVERY
======================================================

1. Analyze the entire project.

2. Build a dependency graph of all Dart files.

3. Identify:

- broken imports
- duplicate files
- obsolete files
- partially generated files
- orphan files
- unused classes
- unused widgets
- duplicated services
- duplicated repositories
- duplicated models
- invalid exports

4. Delete ONLY files that satisfy ALL of the following:

- not referenced anywhere
- completely unused
- duplicate of another implementation
- generated by an incomplete refactor
- impossible to compile
- safe to remove without breaking the remaining project

Never delete files that are still referenced.

======================================================
PHASE 2 – REPAIR
======================================================

Repair the remaining project.

Fix:

- imports
- exports
- package paths
- analyzer errors
- build errors
- missing generated files
- dependency issues

Run:

flutter pub get

dart run build_runner build --delete-conflicting-outputs

dart format .

flutter analyze

Repeat until the analyzer is clean.

======================================================
PHASE 3 – ANALYSIS
======================================================

After the project builds successfully:

Analyze the remaining architecture.

Determine:

- implemented features
- missing features
- missing screens
- missing services
- missing repositories
- missing models
- missing providers
- missing routes
- missing backend integration

Create an internal roadmap.

======================================================
PHASE 4 – IMPLEMENTATION
======================================================

Compare the existing project against the target architecture.

Implement ONLY the missing parts.

Never overwrite a working implementation.

Reuse existing code whenever possible.

Create new files only when necessary.

Maintain a consistent architecture.

======================================================
RULES
======================================================

Never stop after one file.

Never recreate the project.

Never duplicate functionality.

Never create placeholder code.

Always keep the project compiling.

Run flutter analyze after every major change.

Do not continue to the next phase while compile errors still exist.

At the end, provide:

- deleted files
- repaired files
- newly created files
- remaining tasks